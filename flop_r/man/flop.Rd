% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{flop}
\alias{flop}
\title{Run the FLOP causal discovery algorithm.}
\usage{
flop(data, lambdaBic, restarts = NA, timeout = NA, outputDag = FALSE)
}
\arguments{
\item{data}{A data matrix with rows corresponding to observations and columns to variables/nodes.}

\item{lambdaBic}{The penalty parameter of the BIC, a typical value for structure learning is 2.0.}

\item{restarts}{Optional parameter specifying the number of ILS restarts. Either restarts or timeout (below) need to be specified.}

\item{timeout}{Optional parameter specifying a timeout after which the search returns. At least one local search is run up to a local optimum. Either restarts or timeout need to be specified.}

\item{outputDag}{Optional parameter to output a DAG instead of a CPDAG. Default value is FALSE.}
}
\value{
A matrix encoding a CPDAG or DAG. The entry in row i and column j is 1 in case of a directed edge from i to j and 2 in case of an undirected edge between those nodes (an additional 2 in row j and column i is omitted).
}
\description{
For the given data and parameters, the FLOP algorithm is run and returns the learned graph as an adjacency matrix.
}
\examples{
p <- 10
W <- matrix(0, nrow = p, ncol = p)
W[cbind(1:(p-1), 2:p)] <- 1
X <- matrix(rnorm(10000 * p), nrow = 10000, ncol = p) \%*\% solve(diag(p) - W)
X_std <- scale(X)
flop(X, 2.0, restarts=50)
}
